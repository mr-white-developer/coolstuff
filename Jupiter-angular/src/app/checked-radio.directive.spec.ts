import { CheckedRadioDirective } from './checked-radio.directive';

describe('CheckedRadioDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckedRadioDirective();
    expect(directive).toBeTruthy();
  });
});
