import { StkUnitFilterPipe } from './stk-unit-filter.pipe';

describe('StkUnitFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new StkUnitFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
