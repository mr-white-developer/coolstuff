import { StockUnitFilterPipe } from './stock-unit-filter.pipe';

describe('StockUnitFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new StockUnitFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
